attributes  =  "Russel;25;MIS";

var Russel = attributes.split(";");

rus_string = Russel.join(",");

/*
for (attrib of Russel) { //attrib in looks for # of elements, attrib of returns the value
    console.log(attrib);
}
*/
console.log(Russel);
console.log(rus_string);